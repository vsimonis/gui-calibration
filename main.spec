# -*- mode: python -*-
a = Analysis(['nemPyCalibration\\main.py'],
             pathex=['C:\\Users\\Valerie\\Documents\\Visual Studio 2013\\Projects\\NemPyCalibration'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='main.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='main')
